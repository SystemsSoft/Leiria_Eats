package org.leria.eats.project.theme

import androidx.compose.ui.graphics.Color

// ═══════════════════════════════════════════════════════════════════════════════
//  PALETA CENTRAL KOMAAI — Light Theme
//  Todas as telas importam daqui. Não definir cores locais nas screens.
// ═══════════════════════════════════════════════════════════════════════════════

// ── Backgrounds ───────────────────────────────────────────────────────────────
val KomaBg          = Color(0xFFE5ECE5)  // Fundo principal — cinza suave
val KomaSurface     = Color(0x46E8E8E8)  // Superfície branca (cards, sheets, modais)
val KomaCard        = Color(0xFFFFFFFF)  // Cards secundários / campos de input

// ── Primária: Amarelo/Ouro ─────────────────────────────────────────────────────
val KomaGold        = Color(0xFFD7A408)  // CTA principal — botões, badges, FABs
val KomaGoldDark    = Color(0xFFE6A800)  // Pressed / gradiente escuro do ouro
val KomaGoldOnDark  = Color(0xFF5C3D00)  // Texto/ícone escuro sobre fundo dourado

// ── Verde marca — acentos e ícones ────────────────────────────────────────────
val KomaBrandGreen  = Color(0xFF1A7A5E)  // Verde vivo para preços, ícones, acentos
val KomaMintLight   = Color(0xFFE8F5F0)  // Verde muito claro — bubble IA, chips

// ── Texto ─────────────────────────────────────────────────────────────────────
val KomaTextPrimary = Color(0xFF111827)  // Texto principal (títulos, nomes)
val KomaTextSec     = Color(0xFF6B7280)  // Texto secundário (subtítulos, muted)
val KomaTextHint    = Color(0xFFD1D5DB)  // Placeholder / texto desativado

// ── Utilitários ───────────────────────────────────────────────────────────────
val KomaDivider     = Color(0xFFE5E7EB)  // Linhas divisórias / borders suaves
val KomaError       = Color(0xFFDC2626)  // Erros e estados críticos
val KomaStarYellow  = Color(0xFFFFB800)  // Estrelas de avaliação
val KomaWhite       = Color(0xFFFFFFFF)
val KomaBlack       = Color(0xFF111827)

// ── Smart / Destaque ──────────────────────────────────────────────────────────
val KomaSmartGold     = Color(0xFFFFD700)  // Borda/badge restaurantes SMART
val KomaSmartGoldDark = Color(0xFFB8860B)  // Gradiente escuro SMART
val KomaSmartCardBg   = Color(0xFFFFFDE7)  // Fundo card SMART (amarelo muito claro)

// ── Cores específicas de UI ───────────────────────────────────────────────────
val KomaClosedRed     = Color(0xFFB71C1C)  // Badge "FECHADO"
val KomaOrangeEnd     = Color(0xFFE65100)  // Fim de gradiente laranja (CTAs)
val KomaGreenDark     = Color(0xFF15803D)  // Verde escuro (gradiente botão Produtos)
val KomaGoldAccent    = Color(0xFFFFD54F)  // Âmbar claro (badge IA gradiente)
val KomaLimeGreen     = Color(0xFF84CC16)  // Verde lima (gradiente botão Adicionar)
val KomaSoftRed       = Color(0xFFF87171)  // Vermelho suave (erros inline, delete)

// ── Status de pedidos ─────────────────────────────────────────────────────────
val KomaStatusPendente  = Color(0xFFFFC107)  // Pendente — amarelo
val KomaStatusPreparo   = Color(0xFF60A5FA)  // Em Preparo — azul
val KomaStatusEntrega   = Color(0xFF22C55E)  // estafeta chegou ao seu endereco — verde vivo
val KomaStatusEntregue  = Color(0xFFFFD54F)  // Entregue — âmbar
val KomaStatusRecolha   = Color(0xFF22C55E)  // Pronto para Recolha — verde vivo
val KomaStatusCancelado = Color(0xFFF87171)  // Cancelado — vermelho suave
val KomaStatusDefault   = Color(0xFF6B7280)  // Default — cinza

